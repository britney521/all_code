let element = document.getElementsByClassName('comments--a9Uyym1a beautify-scroll-bar')[1]
element.scrollTo({
    top: element.scrollHeight,
    behavior: 'smooth' // 可选，平滑滚动
  });


let element = document.getElementsByClassName('_list_1ygkr_67')[0]
element.scrollTo({
    top: element.scrollHeight,
    behavior: 'smooth' // 可选，平滑滚动
  });
